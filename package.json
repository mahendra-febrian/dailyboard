{
  "name": "dailyboard",
  "version": "1.0.0",
  "description": "Dailyboard — dashboard produktivitas harian berisi daftar tugas (to-do list), catatan cepat (notes), dan widget cuaca, seluruhnya dibangun dengan HTML, CSS, dan Javascript murni.",
  "keywords": [],
  "license": "ISC",
  "author": "Mahendra Febrian",
  "type": "module",
  "main": "dist/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild *.js --bundle --minify --outdir=dist/ && esbuild style.css --minify --outfile=dist/style.css"
  },
  "devDependencies": {
    "@eslint/css": "^1.4.0",
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.28.2",
    "eslint": "^10.8.1"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
